# user-service (Django REST)

Python package manager: **UV**

This service implements the user domain and authentication endpoints.

## Quickstart (local / VPS)

1. Copy central env: `cp ../../.env .env`
2. Install deps (using UV):
   ```bash
   uv install -r requirements.txt
   ```
